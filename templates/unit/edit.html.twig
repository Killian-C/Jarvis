{% extends 'base.html.twig' %}

{% block title %}Mettre à jour l'unité de mesure{% endblock %}

{% block body %}
    <div class="container">
        <a href="{{ path('unit_index') }}">Retour à la liste des unités de mesures</a>
        <h4 class="text-center text-primary-blue mt-3">Editer l'unité de mesure</h4>
        <div class="d-flex align-items-center flex-column mt-3">
            {{ include('unit/_form.html.twig', {'button_label': 'Update'}) }}
            {{ include('unit/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
