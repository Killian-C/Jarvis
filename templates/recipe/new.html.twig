{% extends 'base.html.twig' %}

{% block title %}Nouvelle Recette{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('ingredientEmbedForm') }}
{% endblock %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block body %}
    <a href="{{ path('recipe_index') }}">Retour à la liste des recettes</a>
    <h4 class="text-center">
        {% block title_form %}Ajout d'une nouvelle recette{% endblock %}
    </h4>
    <h6 class="text-center">Toutes les recettes doivent être écrites pour 1 personne</h6>
    <div class="container">
        {{ form_start(form) }}
        <div class="form-box">
            {{ form_row(form.title) }}
            {{ form_row(form.recipeType) }}
            {{ form_row(form.duration) }}
            {{ form_row(form.season) }}
            {{ form_row(form.description) }}
            <ul id="ingredients-container" data-async-ingredients-url="{{ path('aliment_async_search') }}"class="ingredients" data-prototype="{{ form_widget(form.ingredients.vars.prototype)|e('html_attr') }}">
                {% for ingredient in form.ingredients %}
                    <li>
                        {{ form_widget(ingredient) }}
                    </li>
                {% endfor %}
            </ul>
            <button id="add-ingredient" type="button" class="add_item_link btn btn-secondary" data-collection-holder-class="ingredients">Ajouter un ingrédient</button>
        </div>
        <div class="text-center my-5">
            <input class="btn btn-primary p-3" type="submit" value="{% block text_button %}Ajouter{% endblock %}">
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}