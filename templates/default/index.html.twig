{% extends 'base.html.twig' %}

{% block title %}On mange quoi c'soir ?{% endblock %}
{% block navbar %}{% endblock %}
{% block body %}
    <div class="home-page d-flex flex-column align-items-center justify-content-center pb-3">
        <div class="d-flex align-items-center justify-content-center flex-wrap">
            <a class="p-2 btn btn-secondary text-secondary-dark m-2" href="{{ path('aliment_index') }}">
                <i class="fas fa-pepper-hot mr-1"></i>
                Aliments
            </a>
            <a class="p-2 btn btn-secondary text-secondary-dark m-2" href="{{ path('recipe_index') }}">
                <i class="fas fa-book mr-1"></i>
                Recettes
            </a>
            <a class="p-2 btn btn-secondary text-secondary-dark m-2" href="{{ path('menu_index') }}">
                <i class="fas fa-table mr-1"></i>
                Menus
            </a>
        </div>
        <div class="text-center mt-4 mb-3">
            <h1 class="text-break w-80 mx-auto">On mange quoi c'soir ?</h1>
            <p>Un menu, une liste de courses.</p>
        </div>
        <div class="main-menu-cta-box">
            <div class="d-flex align-items-center justify-content-around main-cta">
                <a class="btn btn-tertiary text-secondary-dark p-3" href="{{ path('menu_new') }}">
                    <i class="fas fa-scroll mr-2"></i>
                    Créer un menu
                </a>
            </div>
            <div class="d-flex align-items-center justify-content-around main-cta">
                <a class="btn btn-tertiary text-secondary-dark p-3" href="{{ path('shopping_list_index') }}">
                    <i class="fas fa-tasks mr-2"></i>
                    LA Liste de courses
                </a>
            </div>
        </div>
    </div>
{% endblock %}
